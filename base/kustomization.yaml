apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: deltahouse

resources:
#  - pv-database.yaml
#  - database.yaml
  - service.yaml
  - deployment.yaml
#  - http-route.yaml
  - ingress-route.yaml

#configMapGenerator:
#  - name: configuration
#    files:
#      - configuration.yaml

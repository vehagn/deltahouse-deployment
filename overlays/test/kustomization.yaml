apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: delta-test
commonLabels:
  app: delta-test

resources:
  - ../../base

patches:
  - path: deployment.yaml
  - path: ingress.yaml